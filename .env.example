# No environment variables are required for the curated MVP.
#
# Optional future AI fallback variables must remain server-only. Do not use a
# NEXT_PUBLIC_ prefix for provider keys.
# OPENAI_API_KEY=
